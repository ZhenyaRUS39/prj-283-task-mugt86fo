# Plan: prj-283-task-mugt86fo

**Project:** `PRJ-3087`  
**Task ID:** `task-mugt86fo`  
**Repo:** `prj-283-task-mugt86fo`  

## Summary

Декомпозирую задачу "Скрипт парсинга XML v2" на 4 этапа: исследование формата → реализация парсера → валидация и тесты → документация и упаковка в PR.

## Subtasks

### 1. Исследовать требования и формат XML

- **ID:** `sub-1`
- **Profile:** `20razrab1`
- **Tester:** `TBD`
- **Status:** `pending`
- **Description:** Найти примеры XML-файлов для парсинга, зафиксировать схему (XSD или описание полей), определить целевую структуру вывода (JSON/dict/DataFrame). Результат — docs/schema.md с описанием полей и примерами.
- **Test plan:** В docs/schema.md присутствуют: список полей с типами, минимум 2 примера XML, пример выходной структуры. Схема согласуется с форматом входных файлов из репо.

### 2. Реализовать скрипт парсинга XML v2

- **ID:** `sub-2`
- **Profile:** `30razrab2`
- **Tester:** `TBD`
- **Status:** `pending`
- **Description:** Написать xml_parser_v2.py (или .ts) с CLI/функцией parse(path, options). Поддержать namespaces, атрибуты, вложенность. Добавить обработку ошибок и логирование. Вернуть структурированный результат согласно docs/schema.md.
- **Test plan:** Скрипт запускается на 2+ тестовых XML из samples/, выдаёт корректный dict/JSON. python xml_parser_v2.py sample.xml --pretty отрабатывает без traceback.
- **Dependencies:** `sub-1`

### 3. Написать unit-тесты и валидацию

- **ID:** `sub-3`
- **Profile:** `20razrab1`
- **Tester:** `TBD`
- **Status:** `pending`
- **Description:** Покрыть pytest/юнит-тестами: успешный парсинг, невалидный XML, пустой файл, namespaces, граничные случаи. Добавить CI-friendly запуск (pytest -q). Минимум 8 тестов, coverage парсера ≥80%.
- **Test plan:** pytest -q возвращает все тесты passed. coverage report показывает ≥80% для xml_parser_v2. Есть тесты на: валидный XML, невалидный XML, отсутствие файла, namespaces, пустые теги.
- **Dependencies:** `sub-2`

### 4. Документация и подготовка PR

- **ID:** `sub-4`
- **Profile:** `30razrab2`
- **Tester:** `TBD`
- **Status:** `pending`
- **Description:** Обновить README.md с примерами запуска, описанием CLI-флагов и структурой вывода. Подготовить CHANGELOG.md с записью v2.0.0. Сформировать diff для Айтишника: feat/xml-parser-v2 branch + git diff origin/main..HEAD --stat.
- **Test plan:** README содержит: quickstart, CLI-флаги, пример ввода/вывода, ссылку на schema.md. git diff --stat показывает изменённые файлы. PR готов к ревью Айтишника.
- **Dependencies:** `sub-3`


---
*Generated by Hermes Orchestrator*
